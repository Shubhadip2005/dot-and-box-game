{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['gridSize', 'maxPlayers', 'status', 'hostId', 'players'])"
      }
    }
  }
}
